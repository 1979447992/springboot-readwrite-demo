# PostgreSQL主库配置
listen_addresses = '*'
port = 5432
max_connections = 100

# 启用流复制
wal_level = replica
max_wal_senders = 3
max_replication_slots = 3
wal_keep_size = 1GB

# 日志配置
logging_collector = on
log_statement = 'all'
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '